[project]
name = "machine-learning-implementations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.0",
]

[dependency-groups]
app = [
    "streamlit>=1.52.2",
]
notebooks = [
    "jupyter>=1.1.1",
]
